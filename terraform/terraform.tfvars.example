# Copy thành terraform.tfvars rồi điền. File terraform.tfvars đã bị .gitignore.

# curl -4 ifconfig.me  → thêm /32
my_ip = "x.x.x.x/32"

# Các giá trị dưới có default hợp lý — chỉ override khi cần
# region        = "ap-southeast-1"
# instance_type = "t3.large"
# ssh_public_key_path = "~/.ssh/uav-lab.pub"
